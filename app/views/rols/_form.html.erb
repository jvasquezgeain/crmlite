<%= form_with(model: rol) do |form| %>
  <% if rol.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rol.errors.count, "error") %> prohibited this rol from being saved:</h2>

      <ul>
        <% rol.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name_of, style: "display: block" %>
    <%= form.text_field :name_of %>
  </div>

  <div>
    <%= form.label :stated_at, style: "display: block" %>
    <%= form.check_box :stated_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
